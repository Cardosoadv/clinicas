.env

# writable/: mantém a árvore de diretórios versionada (via .gitkeep),
# mas ignora todo o conteúdo gerado em runtime (cache, logs, sessões,
# uploads, imagens/avatares por paciente/loja etc.), em qualquer profundidade.
writable/**
!writable/**/
!writable/**/.gitkeep

vendor/*
build/
